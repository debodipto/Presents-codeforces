#include <iostream>
#include <vector>

using namespace std;

int main()
{
    int n, p;
    cin >> n;
    vector<int> k(n+1);
    for (int i = 1; i <= n; ++i)
    {
        cin >> p;
        k[p] = i;
    }
    cout << k[1];
    for (int i = 2; i <= n; ++i)
    {
        cout << " " << k[i];
    }
    cout << endl;
    return 0;
}
